# Reproduction pipeline dependencies (Python 3.10).
# A CUDA-enabled torch is recommended (DLinear trains on GPU); CPU also works.
numpy>=2.2
pandas>=2.2
torch>=2.6
lightgbm>=4.6
scikit-learn>=1.7
tqdm>=4.66
